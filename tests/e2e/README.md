# End-to-end tests

Put tests here when they require real external state, such as Docker, SDK
containers, network services, device hardware, or credentials.

Run them explicitly with:

```bash
./scripts/run-tests.sh e2e
```
